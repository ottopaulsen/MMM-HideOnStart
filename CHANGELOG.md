Changes worth noticing, per version

### 1.0.0

- First version published.
- Hide on `DOM_OBJECTS_CREATED` instead of `ALL_MODULES_STARTED`, so the
  modules are really hidden and not just flagged as hidden.
- Hide without a lock string, so the modules can be shown again by others.
